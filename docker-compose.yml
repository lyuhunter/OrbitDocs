services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./config.toml:/app/config.toml
      - ./content:/app/content
    environment:
      - NODE_ENV=production
    restart: unless-stopped
